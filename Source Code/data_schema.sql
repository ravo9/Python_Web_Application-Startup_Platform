DROP TABLE if EXISTS circles;

CREATE TABLE circles (
  id int,
  leader_mail text,
  status text,
  title text,
  desc text,
  public_date date,
  expiry_date date,
  investment_value int,
  joined_members text,
  interested_memebers text
);
